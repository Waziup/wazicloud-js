import { Waziup } from "./waziup";
/**
 * The Waziup Cloud has the same API as a Waziup Gateway, so this Cloud type is an alias to the [[Gateway]] type: they are both [[Waziup]].
 */
export type Cloud = Waziup;